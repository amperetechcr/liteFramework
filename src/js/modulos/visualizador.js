(function () {    "use strict";    var MAPEO_ROLES = {        "orquestador de flujo": "Valeria",        orquestador:            "Valeria",        "project manager":      "Laura",        pm:                     "Laura",        "data analyst":         "Lucia",        data_analyst:           "Lucia",        "opencode bridge":      "Agustin",        opencode_bridge:        "Agustin",    };    function _actualizarConfig(data) {        if (!data) return;        if (data.role_map) {            Object.keys(data.role_map).forEach(function(role) {                var name = data.role_map[role];                var lower = role.toLowerCase().trim();                MAPEO_ROLES[lower] = name;                MAPEO_ROLES[lower.replace(/ /g, '_')] = name;            });        }        if (data.destinos_similares) DESTINOS_SIMILARES = data.destinos_similares;        if (data.canvas) { Object.assign(CONFIG, data.canvas); CONFIG.TEXTO_CLARO = data.canvas.texto_claro || '#e8e8f0'; CONFIG.TAM_NPC = data.canvas.tam_npc || 10.4; }        if (data.tonos_piel) TONOS_PIEL = data.tonos_piel;        if (data.estaciones) ESTACIONES = data.estaciones;        if (data.slots) SLOTS = data.slots;        if (data.acciones) ACCIONES = data.acciones;        if (data.dialogos) DIALOGOS = data.dialogos;    }    function _cargarConfig() {        var apiBase = (document.querySelector('meta[name="api-base"]')?.content || '').replace(/\/+$/, '');        var configUrl = apiBase ? apiBase + '/crewai/config' : (window.PIXELAMP_DATA?.URL_BASE || '') + '/api/crewai/config';        fetch(configUrl, { headers: { 'Accept': 'application/json' } })            .then(function(r) { return r.json(); })            .then(function(data) {                _actualizarConfig(data);                if (data.agentes) {                    AGENTES = data.agentes;                }            })            .catch(function(err) {                console.log('[PixelAmpCrew] Config fetch fallback a defaults:', err);            })            .finally(function() {                seleccionarActivosIniciales();                actualizarPanel();                loop();                iniciarStreamSSE();            });    }    var DESTINOS_SIMILARES = {        "escritorio":  ["escritorio1","escritorio2","escritorio3","escritorio4"],        "reunion":     ["reunion"],        "sala reunion":["reunion"],        "reuniones":   ["reunion"],        "servidor":    ["servidor"],        "server":      ["servidor"],        "juegos":      ["juegos"],        "gaming":      ["juegos"],        "descanso":    ["descanso"],        "break":       ["descanso"],        "cafe":        ["descanso"],        "biblioteca":  ["biblioteca"],        "libreria":    ["biblioteca"],        "libros":      ["biblioteca"],    };    function normalizarDestino(destino) {        if (!destino) return "escritorio1";        var d = destino.toLowerCase().trim();        var mapeo = DESTINOS_SIMILARES[d];        if (mapeo) return mapeo[Math.floor(Math.random() * mapeo.length)];        var estaciones = ["escritorio1","escritorio2","escritorio3","escritorio4","reunion","servidor","biblioteca","juegos","descanso"];        if (estaciones.indexOf(destino) !== -1) return destino;        return "escritorio1";    }    function encontrarAgentePorEvento(evt) {        var role = (evt.agent_role || evt.rol || "").toLowerCase().trim();        var nombre = evt.nombre || "";        var nombreEsperado = MAPEO_ROLES[role] || MAPEO_ROLES[evt.agent_role] || MAPEO_ROLES[evt.rol] || "";        console.log('[PixelAmpCrew] buscar agente: role="'+role+'" nombre="'+nombre+'" mapeado="'+nombreEsperado+'"');        if (nombreEsperado) {            var encontrado = activos.find(function(a){return a.nombre===nombreEsperado;});            console.log('[PixelAmpCrew] por MAPEO_ROLES:', encontrado ? encontrado.nombre : 'NO ENCONTRADO');            if (encontrado) return encontrado;        }        if (nombre) {            var encontrado = activos.find(function(a){return a.nombre.toLowerCase()===nombre.toLowerCase();});            console.log('[PixelAmpCrew] por nombre:', encontrado ? encontrado.nombre : 'NO ENCONTRADO');            if (encontrado) return encontrado;        }        if (nombre) {            console.log('[PixelAmpCrew] Creando agente dinamico:', nombre, role);            var idx = activos.length;            var newAg = {                id: idx + 1, nombre: nombre, iniciales: nombre.substring(0,2).toUpperCase(),                color: '#' + Math.floor(Math.random()*16777215).toString(16).padStart(6,'0'),                rol: role, crewaiRole: role,                x: 155, y: 285, ox: 0, oy: 0, dx: 0, dy: 0,                progreso: 1, estacion: null, estacionOrigen: null,                slotIdx: undefined, accion: 'idle', emoji: '\uD83D\uDE34', duracion: 0,                inicioViaje: 0, dialogox: 0,            };            activos.push(newAg);            actualizarPanel();            return newAg;        }        console.log('[PixelAmpCrew] No se pudo mapear el evento a ningun agente');        return null;    }    var CONFIG = {        DURACION_MOVER: 1600,        TAM_NPC: 10.4,    };    var AGENTES = window.PIXELAMP_DATA.agentes;    var canvas, ctx, svgContainer;    var ESCALA_X = 1, ESCALA_Y = 1;    var _displayW = 100, _displayH = 100;    var activos = [];    var logEventos = [];    var frame = 0;    var ocupados = {};    var reconnectTimer = null;    var _timerAutonomo = null;    var TONOS_PIEL = ["#f5cba7","#f0c09c","#e8b88c","#f0c8a8","#ecc0a0","#f8d0b0","#e0b090","#d4a87c","#ecc8a8","#f0ccac"];    var ESTACIONES = {        escritorio1: { x: 17.4,  y: 41.8, w: 141.9, h: 79.8, emoji: "\uD83D\uDCBB", label: "Escritorio 1", color: "#2d2d55" },        escritorio2: { x: 168.1, y: 41.8, w: 141.9, h: 79.8, emoji: "\uD83D\uDCBB", label: "Escritorio 2", color: "#2d2d55" },        escritorio3: { x: 318.6, y: 41.8, w: 141.9, h: 79.8, emoji: "\uD83D\uDCBB", label: "Escritorio 3", color: "#2d2d55" },        escritorio4: { x: 469.3, y: 41.8, w: 133.3, h: 79.8, emoji: "\uD83D\uDCBB", label: "Escritorio 4", color: "#2d2d55" },        reunion: { x: 17.4,  y: 140.6, w: 292.6, h: 83.6, emoji: "\uD83D\uDCAC", label: "Sala Reuniones",  color: "#2d2d55" },        servidor: { x: 17.4,  y: 239.4, w: 185.3, h: 136.8, emoji: "\uD83D\uDDBB", label: "Servidor",       color: "#2d2d55" },        juegos: { x: 426.6, y: 239.4, w: 176, h: 136.8, emoji: "\uD83C\uDFAE", label: "Gaming Zone",    color: "#2d2d55" },        descanso: { x: 327.4, y: 140.6, w: 275.2, h: 83.6, emoji: "\u2615",       label: "Break & Lounge", color: "#2d2d55" },        biblioteca: { x: 219.4, y: 239.4, w: 189.1, h: 136.8, emoji: "\uD83D\uDCDA", label: "Biblioteca",    color: "#2d2d55" },    };    var SLOTS = {        escritorio1: [[88, 81.2]],        escritorio2: [[238, 81.2]],        escritorio3: [[389.4, 81.2]],        escritorio4: [[535.6, 81.2]],        reunion: [[47.2,174.8],[99.2,174.8],[150.7,174.8],[202.2,174.8],[254.2,174.8],[47.2,197.6],[99.2,197.6],[150.7,197.6],[202.2,197.6],[254.2,197.6]],        servidor: [[77.5,304],[137.6,304]],        juegos: [[499.8,304],[551.1,304]],        descanso: [[370.1,197.6],[439,197.6],[507.7,197.6]],        biblioteca: [[284.5,304],[361.5,304]],    };    var ACCIONES = [        { accion: "codificando",  emoji: "\u2328\uFE0F", destinos: ["escritorio1","escritorio2","escritorio3","escritorio4"], duracion: 7000 },        { accion: "revisando",    emoji: "\uD83D\uDD0D",  destinos: ["escritorio1","escritorio2","escritorio3","escritorio4"], duracion: 5500 },        { accion: "documentando", emoji: "\uD83D\uDCDD",  destinos: ["escritorio1","escritorio2","escritorio3","escritorio4"], duracion: 6000 },        { accion: "leyendo",      emoji: "\uD83D\uDCD6",  destinos: ["biblioteca"], duracion: 8000 },        { accion: "reunion",      emoji: "\uD83D\uDCAC",  destinos: ["reunion"], duracion: 10000 },        { accion: "servidor",     emoji: "\uD83D\uDDBB",  destinos: ["servidor"], duracion: 5000 },        { accion: "jugando",      emoji: "\uD83C\uDFAE",  destinos: ["juegos"], duracion: 6000 },        { accion: "cafe",         emoji: "\u2615",        destinos: ["descanso"], duracion: 4000 },        { accion: "descanso",    emoji: "\u2615",        destinos: ["descanso"], duracion: 4000 },    ];    var DIALOGOS = {        "Orquestador de Flujo": ["Analizando el prompt","Revisando resultados","Flujo completado","Delegando tareas","Consolidando respuesta","Todo en orden","Siguiente fase","Reporte listo"],        "Project Manager":      ["Revisemos la arquitectura","El deadline es viernes","Necesito el reporte ASAP","Esto va a produccion hoy","Prioridad: estabilidad","Revise ese PR","Tenemos tech debt aqui","Alguien vio mi commit?"],        "Data Analyst":         ["Pipeline de datos OK","Dashboard actualizado","Metricas consistentes","ETL completado","Anomalia detectada","Reporte generado","Modelo entrenado","Accuracy 94%"],        "OpenCode Bridge":      ["Archivo leido","Escribiendo cambios","Comando ejecutado","Llamada MCP exitosa","Filtrando contenido","Token reducido","Resultado obtenido","Listo, siguiente paso"],        "Data":                 ["Pipeline de datos OK","Dashboard actualizado","Metricas consistentes","ETL completado","Anomalia detectada","Reporte generado","Modelo entrenado","Accuracy 94%"],        "General":              ["Alguien pidio cafe?","Eso fue commit ciego","Listo, lo muevo a done","Me llevo esto al break","Quien revisa mi PR?","Necesito ayuda aca","Ya casi termino","Bug o feature?"],        "codificando":          ["Commit en camino","Test verde","Push a develop","WIP limit respetado","Merge sin conflictos","Review pending","Feature completa","Bug squashed"],        "revisando":            ["Code review en proceso","Posible NPE aqui","Esto se puede optimizar","LGTM","Necesito mas contexto","Variable mal nombrada","Buena practica","Solo un detalle"],        "documentando":         ["README actualizado","API docs generados","Swagger sincronizado","Changelog al dia","Wiki actualizada","Comentarios utiles","Guia de inicio","Arquitectura documentada"],        "cafe":                 ["Cafe pendiente","Alguien mas quiere?","Cafe recien hecho","Descanso mental","Recargando energia","Justo lo necesitaba","Se acabo el cafe","Segunda taza"],    };    function rand(m, M) { return Math.floor(Math.random() * (M - m + 1)) + m; }    function elegir(a) { return a[rand(0, a.length - 1)]; }    function oscurecer(hex, factor) {        var r = parseInt(hex.slice(1,3),16), g = parseInt(hex.slice(3,5),16), b = parseInt(hex.slice(5,7),16);        return "rgb(" + Math.floor(r*(1-factor)) + "," + Math.floor(g*(1-factor)) + "," + Math.floor(b*(1-factor)) + ")";    }    function ocuparSlot(estId) {        var slots = SLOTS[estId];
if (!slots) return null;        if (!ocupados[estId]) ocupados[estId] = [];        for (var i = 0; i < slots.length; i++) {
if (!ocupados[estId][i]) { ocupados[estId][i] = true; return { slotIdx: i, x: slots[i][0], y: slots[i][1] }; } }        return null;    }    function liberarSlot(ag) {        if (ag.slotIdx === undefined || ag.estacionOrigen === undefined) return;        var e = ag.estacionOrigen;        if (ocupados[e] && ocupados[e][ag.slotIdx]) ocupados[e][ag.slotIdx] = false;    }    function seleccionarActivosIniciales() {        if (!AGENTES || !AGENTES.length) return;        activos = AGENTES.map(function(ag, i) {            var x = 62 + (i % 4) * 136.4;            var y = 342;            return {                id: ag.id, nombre: ag.nombre, iniciales: ag.iniciales,                color: ag.color, rol: ag.rol, crewaiRole: null,                x: x, y: y, ox: x, oy: y, dx: x, dy: y,                progreso: 1, estacion: null, estacionOrigen: null,                slotIdx: undefined,                accion: "idle", emoji: "\uD83D\uDE34", duracion: 0,                inicioViaje: 0, dialogox: i,            };        });    }    function moverA(ag, destinoId, acc) {        console.log('[PixelAmpCrew] moverA:', ag.nombre, 'â†’', destinoId, 'accion:', acc.accion);        liberarSlot(ag);        var slot = ocuparSlot(destinoId);        if (!slot) {            console.log('[PixelAmpCrew] NO HAY SLOT en', destinoId, '- usando posicion fallback');            var est = ESTACIONES[destinoId];            slot = { slotIdx: -1, x: (est ? est.x + est.w/2 : 50), y: (est ? est.y + est.h/2 : 50) };        }        console.log('[PixelAmpCrew] Slot asignado:', destinoId, 'pos:', slot.x, slot.y);        ag.ox = ag.x; ag.oy = ag.y; ag.dx = slot.x; ag.dy = slot.y;        ag.progreso = 0; ag.estacionOrigen = ag.estacion; ag.estacion = destinoId;        ag.slotIdx = slot.slotIdx; ag.accion = acc.accion; ag.emoji = acc.emoji;        ag.duracion = acc.duracion || CONFIG.DURACION_MOVER; ag.inicioViaje = Date.now();    }    function elegirAccion(ag) {        var destinosG = [];        ACCIONES.forEach(function(acc) {            acc.destinos.forEach(function(d) {
if (destinosG.indexOf(d)===-1) destinosG.push(d); });        });        var destino = elegir(destinosG);        var accs = ACCIONES.filter(function(a) { return a.destinos.indexOf(destino) !== -1; });        var acc = elegir(accs);        moverA(ag, destino, acc);        agregarLog(ag.nombre, acc.accion, destino);    }    function actualizar() {        var ahora = Date.now();        activos.forEach(function(ag) {            if (ag.progreso < 1) {                var dur = ag.duracion || CONFIG.DURACION_MOVER;                var t = Math.min(1, (ahora - ag.inicioViaje) / dur);                t = t < 0.5 ? 2*t*t : -1+(4-2*t)*t;                ag.x = ag.ox + (ag.dx - ag.ox)*t; ag.y = ag.oy + (ag.dy - ag.oy)*t;                if (t >= 1) { ag.x = ag.dx; ag.y = ag.dy; ag.progreso = 1; }            }        });    }    // FONDO
        function marcarEstacionSVG(estacionId, activa) {        if (!svgContainer) svgContainer = document.getElementById('oficina-svg-container');        if (svgContainer) {            var svg = svgContainer.querySelector('.station.' + estacionId);            if (svg) svg.classList.toggle('active', activa);        }    }    function actualizarEstacionesSVG() {        if (!svgContainer) svgContainer = document.getElementById('oficina-svg-container');        if (!svgContainer) return;        var conteo = {};        activos.forEach(function(a) {            if (a.estacion && a.progreso >= 1) {                conteo[a.estacion] = (conteo[a.estacion] || 0) + 1;            }        });        Object.keys(ESTACIONES).forEach(function(k) {            var svg = svgContainer.querySelector('.station.' + k);            if (!svg) return;            var cnt = conteo[k] || 0;            svg.classList.toggle('active', cnt > 0);            var countEl = svg.querySelector('.station-count');            if (countEl) {                if (k === 'reunion') countEl.textContent = cnt + ' en reunion';                else if (k === 'descanso') countEl.textContent = '\u2615 ' + cnt;                else if (k === 'juegos') countEl.textContent = '\uD83C\uDFAE ' + cnt;            }        });    }    function redimensionarCanvas() {        var contenedor = canvas.parentElement;        if (!contenedor) return;        var rect = contenedor.getBoundingClientRect();        _displayW = rect.width;        _displayH = rect.height;        var dpr = window.devicePixelRatio || 1;        canvas.width = _displayW * dpr;        canvas.height = _displayH * dpr;        ctx.setTransform(1,0,0,1,0,0);        ctx.scale(dpr, dpr);    }// NPCS
        function dialogoPara(ag) {        if (!DIALOGOS[ag.rol] && !DIALOGOS.General) { console.warn("[PixelAmpCrew] No hay dialogos para rol="+ag.rol, ag); return "trabajando..."; }        var lines = DIALOGOS[ag.rol] || DIALOGOS.General;        if (DIALOGOS[ag.accion]) lines = DIALOGOS[ag.accion];        if (!lines || !lines.length) { console.warn("[PixelAmpCrew] lines vacio para", ag); return "descansando"; }        if (typeof ag.dialogox !== "number" || ag.dialogox >= lines.length) ag.dialogox = rand(0, lines.length-1);        var txt = lines[ag.dialogox];        if (txt === undefined) { console.warn("[PixelAmpCrew] dialogox="+ag.dialogox+" undefined en lines", lines, ag); txt = "trabajando..."; }        return txt;    }    function dibujarBurbuja(cx, cy, texto, color) {        ctx.save(); ctx.font="bold 11px sans-serif";        var ancho=ctx.measureText(texto).width+18, alto=22;        ctx.shadowColor="rgba(0,0,0,0.3)"; ctx.shadowBlur=4;        ctx.fillStyle=color;
var bx=cx-ancho/2;
var by=cy-CONFIG.TAM_NPC/2-alto-10;        ctx.beginPath(); ctx.roundRect(bx,by,ancho,alto,6); ctx.fill();        ctx.shadowBlur=0; ctx.strokeStyle="rgba(255,255,255,0.15)"; ctx.lineWidth=1; ctx.beginPath(); ctx.roundRect(bx,by,ancho,alto,6); ctx.stroke();        ctx.fillStyle="#fff"; ctx.font="bold 11px sans-serif"; ctx.textAlign="center"; ctx.textBaseline="middle"; ctx.fillText(texto,cx,by+alto/2);        ctx.fillStyle=color; ctx.beginPath(); ctx.moveTo(cx-5,by+alto); ctx.lineTo(cx,by+alto+6); ctx.lineTo(cx+5,by+alto); ctx.closePath(); ctx.fill();        ctx.restore();    }    function dibujarNPC(ag) {        var t = CONFIG.TAM_NPC;        var cx = ag.x, cy = ag.y;        var caminando = ag.progreso < 1;        var bobY = caminando ? Math.sin(frame*0.12+ag.id)*2 : 0;        var idle = ag.accion === "idle" || ag.accion === undefined;        if (!idle && ag.progreso >= 1) {            if (ag.estacion && ag.estacion.indexOf("escritorio")===0 && ag.dialogox%2===0 && Math.sin(frame*0.02+ag.id)>0.3) {                var txt = dialogoPara(ag);                dibujarBurbuja(cx, cy+bobY, txt, ag.color);            } else if (ag.estacion==="reunion") {                var txt = dialogoPara(ag);                var alpha = 0.6+Math.sin(frame*0.04+ag.id)*0.3;                ctx.globalAlpha = Math.max(0.3, alpha);                dibujarBurbuja(cx, cy+bobY, txt, ag.color);                ctx.globalAlpha = 1;            }        }        ctx.save();        if (idle) { ctx.globalAlpha = 0.7; }        ctx.shadowColor = ag.color+"44"; ctx.shadowBlur = 6; ctx.shadowOffsetY = 2;        var tonoPiel = TONOS_PIEL[ag.id%TONOS_PIEL.length];        var m = t/2;        var anchoHombro = m*0.85;        var anchoCintura = m*0.47;        var cabezaR = m*0.65;        var cxC = cx, cyC = cy+bobY - m*0.3;        ctx.strokeStyle = tonoPiel; ctx.lineWidth = 4; ctx.lineCap = "round";        ctx.fillStyle = tonoPiel;        ctx.beginPath(); ctx.arc(cxC, cyC, cabezaR, 0, Math.PI*2); ctx.fill();        ctx.fillStyle = oscurecer(ag.color,0.5);        ctx.beginPath(); ctx.arc(cxC, cyC-cabezaR*0.15, cabezaR*1.05, Math.PI, 0); ctx.fill();        ctx.fillStyle = "#fff";        ctx.beginPath(); ctx.arc(cxC-cabezaR*0.32, cyC-cabezaR*0.05, cabezaR*0.22, 0, Math.PI*2); ctx.fill();        ctx.beginPath(); ctx.arc(cxC+cabezaR*0.32, cyC-cabezaR*0.05, cabezaR*0.22, 0, Math.PI*2); ctx.fill();        ctx.fillStyle = "#333";        ctx.beginPath(); ctx.arc(cxC-cabezaR*0.32, cyC-cabezaR*0.05, cabezaR*0.1, 0, Math.PI*2); ctx.fill();        ctx.beginPath(); ctx.arc(cxC+cabezaR*0.32, cyC-cabezaR*0.05, cabezaR*0.1, 0, Math.PI*2); ctx.fill();        ctx.fillStyle = "#fff";        ctx.beginPath(); ctx.arc(cxC-cabezaR*0.28, cyC-cabezaR*0.1, cabezaR*0.04, 0, Math.PI*2); ctx.fill();        ctx.beginPath(); ctx.arc(cxC+cabezaR*0.38, cyC-cabezaR*0.1, cabezaR*0.04, 0, Math.PI*2); ctx.fill();        ctx.fillStyle = "rgba(255,150,150,0.25)";        ctx.beginPath(); ctx.arc(cxC-cabezaR*0.4, cyC+cabezaR*0.2, cabezaR*0.12, 0, Math.PI*2); ctx.fill();        ctx.beginPath(); ctx.arc(cxC+cabezaR*0.4, cyC+cabezaR*0.2, cabezaR*0.12, 0, Math.PI*2); ctx.fill();        ctx.strokeStyle = "#555"; ctx.lineWidth = 1.2;        ctx.beginPath(); ctx.arc(cxC, cyC+cabezaR*0.35, cabezaR*0.18, 0.1, Math.PI-0.1); ctx.stroke();        ctx.fillStyle = tonoPiel; ctx.fillRect(cxC-3, cyC+cabezaR*0.85, 6, 5);        var cuerpoY = cyC+cabezaR*0.95;        var cuerpoH = m*0.95;        ctx.fillStyle = ag.color;        ctx.beginPath(); ctx.moveTo(cxC-anchoHombro, cuerpoY);        ctx.quadraticCurveTo(cxC, cuerpoY+5, cxC+anchoHombro, cuerpoY);        ctx.lineTo(cxC+anchoCintura, cuerpoY+cuerpoH);        ctx.quadraticCurveTo(cxC, cuerpoY+cuerpoH+3, cxC-anchoCintura, cuerpoY+cuerpoH);        ctx.closePath(); ctx.fill();        if (ag.rol==="Tech Lead") {            ctx.fillStyle = "#dc2626"; ctx.fillRect(cxC-2, cuerpoY+8, 4, cuerpoH-16);            ctx.fillStyle = "#fff"; ctx.fillRect(cxC-3, cuerpoY+12, 6, 2);        }        if (ag.rol==="Backend") {            ctx.strokeStyle = "#aaa"; ctx.lineWidth = 1.5;            ctx.beginPath(); ctx.arc(cxC-cabezaR*0.15, cyC-cabezaR*0.05, cabezaR*0.18, 0, Math.PI*2); ctx.stroke();            ctx.beginPath(); ctx.arc(cxC+cabezaR*0.15, cyC-cabezaR*0.05, cabezaR*0.18, 0, Math.PI*2); ctx.stroke();            ctx.strokeStyle = "#aaa"; ctx.lineWidth = 1;            ctx.beginPath(); ctx.moveTo(cxC-cabezaR*0.3, cyC-cabezaR*0.05); ctx.lineTo(cxC+cabezaR*0.3, cyC-cabezaR*0.05); ctx.stroke();        }        if (ag.rol==="DevOps") {            ctx.fillStyle = oscurecer(ag.color,0.4); ctx.beginPath(); ctx.arc(cxC, cyC-cabezaR*0.4, cabezaR*0.7, Math.PI*1.2, Math.PI*1.8); ctx.fill();        }        ctx.lineCap = "round"; ctx.lineWidth = 4.5;        var brazoLen = cuerpoH*0.45;        ctx.strokeStyle = tonoPiel;        ctx.beginPath(); ctx.moveTo(cxC-anchoHombro-2, cuerpoY+6);        ctx.quadraticCurveTo(cxC-anchoHombro-8, cuerpoY+brazoLen*0.5, cxC-anchoHombro-5, cuerpoY+brazoLen); ctx.stroke();        ctx.beginPath(); ctx.moveTo(cxC+anchoHombro+2, cuerpoY+6);        ctx.quadraticCurveTo(cxC+anchoHombro+8, cuerpoY+brazoLen*0.5, cxC+anchoHombro+5, cuerpoY+brazoLen); ctx.stroke();        var piernaLen = cuerpoH*0.2;        ctx.strokeStyle = "#444"; ctx.lineWidth = 4; ctx.lineCap = "square";        ctx.beginPath(); ctx.moveTo(cxC-anchoCintura*0.3, cuerpoY+cuerpoH);        ctx.lineTo(cxC-anchoCintura*0.3, cuerpoY+cuerpoH+piernaLen); ctx.stroke();        ctx.beginPath(); ctx.moveTo(cxC+anchoCintura*0.3, cuerpoY+cuerpoH);        ctx.lineTo(cxC+anchoCintura*0.3, cuerpoY+cuerpoH+piernaLen); ctx.stroke();        ctx.fillStyle = "#333";        ctx.beginPath(); ctx.roundRect(cxC-anchoCintura*0.3-3, cuerpoY+cuerpoH+piernaLen-2, 6, 4, 2); ctx.fill();        ctx.beginPath(); ctx.roundRect(cxC+anchoCintura*0.3-3, cuerpoY+cuerpoH+piernaLen-2, 6, 4, 2); ctx.fill();        ctx.shadowBlur = 0; ctx.shadowOffsetY = 0;        if (!idle) {            ctx.font = "16px sans-serif"; ctx.textAlign = "center"; ctx.textBaseline = "bottom";            ctx.fillStyle = ag.color; ctx.globalAlpha = 0.9+Math.sin(frame*0.06+ag.id*2)*0.1;            ctx.fillText(ag.emoji, cx, cy+bobY-t/2-2); ctx.globalAlpha = 1;        }        ctx.shadowColor = "rgba(0,0,0,0.4)"; ctx.shadowBlur = 3; ctx.shadowOffsetY = 1;        ctx.font = "10px sans-serif"; ctx.fillStyle = CONFIG.TEXTO_CLARO; ctx.textBaseline = "top";        ctx.fillText(ag.nombre, cx, cy+bobY+t/2+2);        ctx.shadowBlur = 0; ctx.shadowOffsetY = 0;        ctx.restore();    }    // DIBUJAR
        function dibujar() {        CONFIG.TAM_NPC = 17.1;        ctx.clearRect(0, 0, canvas.width, canvas.height);        ctx.save();        ctx.scale(_displayW / 620, _displayH / 380);        activos.forEach(dibujarNPC);        ctx.restore();    }    // CICLO
        function procesarEventoCrewAI(evt) {        console.log('[PixelAmpCrew] procesarEventoCrewAI:', JSON.stringify(evt));        var ag = encontrarAgentePorEvento(evt);        if (!ag) {            console.log('[PixelAmpCrew] No se encontro agente para evento:', JSON.stringify(evt));            return;        }        console.log('[PixelAmpCrew] Agente encontrado:', ag.nombre, 'accion actual:', ag.accion);        ag.crewaiRole = evt.agent_role || evt.rol || ag.crewaiRole;        ag.accionAnterior = ag.accion;        var accNombre = evt.accion || "";        var acc = ACCIONES.find(function(a) { return a.accion === accNombre; });        if (!acc) {            if (accNombre) {                acc = {accion:accNombre, emoji: evt.emoji || "\u2699\uFE0F", destinos:["escritorio1","escritorio2","escritorio3","escritorio4"], duracion:5000};                ACCIONES.push(acc);            } else {                acc = ACCIONES[0];            }        }        var destino = normalizarDestino(evt.destino);        console.log('[PixelAmpCrew] Mover a:', ag.nombre, 'â†’', destino, 'accion:', acc.accion);        moverA(ag, destino, acc);        if (evt.mensaje) {            ag.dialogox = DIALOGOS.General.indexOf(evt.mensaje);            if (ag.dialogox === -1 && DIALOGOS.General.length < 50) {                DIALOGOS.General.push(evt.mensaje);                ag.dialogox = DIALOGOS.General.length - 1;            } else if (ag.dialogox === -1) {                ag.dialogox = 0;            }        }        agregarLog(ag.nombre, acc.accion, destino);        actualizarPanel();        actualizarEstacionesSVG();    }    function agregarLog(nombre,accion,lugar) { logEventos.unshift({nombre:nombre,accion:accion,lugar:lugar,tiempo:new Date()});
if (logEventos.length>8) logEventos.pop(); }    function actualizarPanel() {        var dA=document.getElementById("panel-agentes");        if (dA) {            var activosOrden = activos.slice().sort(function(a,b){return (a.accion==="idle"?1:0) - (b.accion==="idle"?1:0) || a.nombre.localeCompare(b.nombre);});            dA.innerHTML = activosOrden.map(function(a){                var idle = a.accion === "idle" || a.accion === undefined;                var emoji = idle ? "\uD83D\uDE34" : (a.progreso<1 ? "\uD83D\uDEB6" : a.emoji);                var estilo = idle ? "opacity:0.4;" : "";                return "<div class=\"panel-agente\" style=\""+estilo+"\"><span class=\"iniciales\" style=\"background:"+a.color+"\">"+a.iniciales+"</span><span class=\"nombre\"><strong>"+a.nombre+"</strong><br><span class=\"texto-xs texto-suave\">"+(a.crewaiRole||a.rol)+"</span></span><span class=\"accion\" title=\""+a.accion+"\">"+emoji+"</span></div>";            }).join("");        }        var dC=document.getElementById("panel-contadores");        if (dC) {            var enAccion=activos.filter(function(a){return a.accion!=="idle" && a.accion!==undefined;}).length;            var r=activos.filter(function(a){return a.estacion==="reunion" && a.accion!=="idle";}).length;            dC.innerHTML = "<div class=\"contador\"><span class=\"numero\" style=\"color:#22c55e\">"+enAccion+"</span><span class=\"etiqueta\">Trabajando</span></div><div class=\"contador\"><span class=\"numero\" style=\"color:#6366f1\">"+r+"</span><span class=\"etiqueta\">Reuni\u00F3n</span></div>";        }        var dL=document.getElementById("log-lista");        if (dL) { dL.innerHTML = logEventos.slice(0,5).map(function(e){var hh=String(e.tiempo.getHours()).padStart(2,"0");var mm=String(e.tiempo.getMinutes()).padStart(2,"0");return "<div class=\"log-item\">"+hh+":"+mm+" \u00B7 <strong>"+e.nombre+"</strong> \u2192 "+e.accion+"</div>";}).join(""); }    }    function loop() { actualizar(); dibujar(); requestAnimationFrame(loop); }    var EVENTOS_VISTOS = {};    var _vistosCount = 0;    function _podarEventosVistos() {        var ahora = Date.now();        var limite = 5 * 60 * 1000;        Object.keys(EVENTOS_VISTOS).forEach(function(k) {            if (ahora - EVENTOS_VISTOS[k] > limite) {                delete EVENTOS_VISTOS[k];            }        });    }    function iniciarStreamSSE() {        var barra = document.getElementById("sse-status");        if (!barra) {            barra = document.createElement("div");            barra.id = "sse-status";            var panel = document.getElementById("panel-lateral-oficina");            if (panel) panel.prepend(barra);        }        var apiBase = (document.querySelector('meta[name="api-base"]')?.content || '').replace(/\/+$/, '');        var streamUrl = apiBase ? apiBase + '/crewai/stream' : (window.PIXELAMP_DATA?.URL_BASE || '') + '/api/crewai/stream';        console.log('[PixelAmpCrew] SSE URL:', streamUrl);        if (window._evtSource) window._evtSource.close();        window._evtSource = new EventSource(streamUrl);        window._evtSource.addEventListener('crewai', function(e) {            try {                var data = JSON.parse(e.data);                if (!data.eventos || !data.eventos.length) return;                barra.textContent = '\u2705 Recibiendo eventos';                data.eventos.forEach(function(evt) {                    var eid = evt._id || '';                    if (EVENTOS_VISTOS[eid]) return;                    EVENTOS_VISTOS[eid] = Date.now();                    _vistosCount++;                    if (_vistosCount > 50) {                        _podarEventosVistos();                        _vistosCount = 0;                    }                    console.log('[PixelAmpCrew] EVENTO SSE:', JSON.stringify(evt));                    procesarEventoCrewAI(evt);                });            } catch(err) {                console.error('[PixelAmpCrew] SSE parse error:', err);            }        });        window._evtSource.onopen = function() {            barra.textContent = '\uD83D\uDFE2 SSE conectado';            barra.className = 'sse-ok';        };        var _sseErrores = 0;        var _fallbackPolling = null;        window._evtSource.onerror = function() {            _sseErrores++;            if (_sseErrores >= 3) {                if (window._evtSource) {                    window._evtSource.close();                    window._evtSource = null;                }                barra.textContent = '\u26A0 SSE fallo, cambiando a polling...';                var apiBase = (document.querySelector('meta[name="api-base"]')?.content || '').replace(/\/+$/, '');                var pollingUrl = apiBase ? apiBase + '/crewai/eventos' : (window.PIXELAMP_DATA?.URL_BASE || '') + '/api/crewai/eventos';                _fallbackPolling = setInterval(function() {                    var controller = new AbortController();                    var timeoutId = setTimeout(function() { controller.abort(); }, 5000);                    fetch(pollingUrl, { headers: { 'Accept': 'application/json' }, signal: controller.signal })                        .then(function(r) { clearTimeout(timeoutId); return r.json(); })                        .then(function(data) {                            if (!data.eventos || !data.eventos.length) return;                            data.eventos.forEach(function(evt) {                                var eid = evt._id || '';                                if (EVENTOS_VISTOS[eid]) return;                                EVENTOS_VISTOS[eid] = Date.now();                                procesarEventoCrewAI(evt);                            });                            barra.textContent = '\u2705 Polling (fallback)';                        })                        .catch(function(err) {                            clearTimeout(timeoutId);                            console.log('[PixelAmpCrew] Polling fallback error:', err);                            barra.textContent = '\u26A0 Fallback error';                        });                }, 3000);                return;            }            barra.textContent = '\uD83D\uDD34 SSE error - reconectando...';            barra.className = 'sse-error';        };    }    function init() {        console.log('[PixelAmpCrew] init() llamado');        canvas=document.getElementById("canvas-oficina");
if (!canvas) return;        console.log('[PixelAmpCrew] Canvas encontrado');        ctx=canvas.getContext("2d");        redimensionarCanvas();        window.addEventListener('resize', redimensionarCanvas);        _cargarConfig();        if (!_timerAutonomo) _timerAutonomo = setInterval(function() { activos.forEach(function(ag) { if (typeof ag.accion !== 'string' || ag.accion === 'idle') elegirAccion(ag); }); }, 10000);    }    if (document.readyState==="loading") document.addEventListener("DOMContentLoaded", init);    else init();})();

